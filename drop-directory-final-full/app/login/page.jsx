'use client';import { useEffect } from 'react';import { useAuth } from '@/components/AuthProvider';import { useRouter } from 'next/navigation';export default function LoginPage(){ const { user, login, ready } = useAuth(); const router = useRouter(); useEffect(()=>{ if(!ready) return; if(user) router.replace('/'); },[user,ready,router]); if(!ready) return <div className='p-6'>Loading…</div>; return (<main className='p-6'><h1 className='text-2xl font-bold mb-4'>Login</h1><p className='mb-4 text-gray-600'>Click below to login as demo user Sagar.</p><button className='rounded-lg bg-indigo-600 px-4 py-2 text-white hover:bg-indigo-700' onClick={()=>{ login(); router.replace('/'); }}>Login as Sagar</button></main>);}
+ import { useAuth } from '../../components/AuthProvider';
